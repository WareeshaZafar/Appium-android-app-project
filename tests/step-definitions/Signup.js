// const { Given, When, Then } = require('@wdio/cucumber-framework');

// Given("The app is launched", async () => {
//   await driver.pause(10000);
// });

// When('The user clicks on the skip button', async () => {

// });

// Then('The user selects the location permission from the popup', async () => {
// });

// Then('The user selects the notifications permission from the popup', async () => {
// });

// Then('The user lands on the login screen', async () => {
// });

// When('The user starts the sign up process by clicking on the Create Personal Account button', async () => {
// });

// Then('The user fills the details and clicks the Register button', async () => {
// });

// When('The user enters the correct OTP on the Verification screen and proceeds', async () => {
// });

// Then('The user lands on the registration screen', async () => {
// });

// When('The user uploads their display picture', async () => {
// });

// When('The user enters valid details for the registration fields and proceeds', async () => {
// });

// Then('The user chooses their city on the Choose City screen and proceeds', async () => {
// });

// Then("The user's new account is successfully registered!", async () => {
// });